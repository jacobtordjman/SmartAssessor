// Optional runtime configuration injected before the app loads.
// Set your public backend URL here when hosting on GitHub Pages.
// Example: window.API_BASE = 'https://your-tunnel-subdomain.trycloudflare.com';
// You can override this at runtime via the browser console:
//   localStorage.setItem('API_BASE', 'https://...')
// Or build-time via Vite: VITE_API_BASE
window.API_BASE = window.API_BASE || '';

